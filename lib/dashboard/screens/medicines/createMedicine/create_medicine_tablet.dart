import 'package:flutter/material.dart';

class CreateMedicineTablet extends StatelessWidget {
  const CreateMedicineTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

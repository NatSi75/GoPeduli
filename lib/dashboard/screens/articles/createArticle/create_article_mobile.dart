import 'package:flutter/material.dart';

class CreateArticleMobile extends StatelessWidget {
  const CreateArticleMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

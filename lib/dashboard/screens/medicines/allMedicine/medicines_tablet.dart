import 'package:flutter/material.dart';

class MedicinesTablet extends StatelessWidget {
  const MedicinesTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class CreateArticleTablet extends StatelessWidget {
  const CreateArticleTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

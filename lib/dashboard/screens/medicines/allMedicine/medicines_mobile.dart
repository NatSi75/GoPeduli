import 'package:flutter/material.dart';

class MedicinesMobile extends StatelessWidget {
  const MedicinesMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
